# 複数レシートの確認

## 実アプリ接続候補を最初から触る

専用統合候補は `fix/receipt-integration-candidate` です。現行mainを起点に、レシート本体、EventDetailsの入口、Functionsの通知入口を固定したスナップショットです。元の作業版やmainは変更しません。

```bash
node tests/preview/receipt-flow/server.mjs
```

開くURL：`http://127.0.0.1:5207/?view=after&width=390`

画面内の画像・OCR・Firestore・通知Callableはローカル代替です。実際の `AddPaymentModal.vue`、保存計算、履歴・取引の生成、結果不明からのサーバー再確認を動かします。課金OCR、本番Firestore、本番通知は呼びません。

現在の統合候補では、複数レシートを別々の履歴・取引・アプリ内お知らせとして扱います。プッシュ通知は登録操作ごと、受信者ごとに1件の要約です。個別のお知らせから `/event/:eventId?history=:historyId` へ進み、該当履歴の詳細を1件だけ開く接続も含みます。

### 5枚を登録する手順

1. 「イベントの支払いを追加」→「複数レシート」を押す。
2. PCでは撮影・アルバムボタンの枠へ画像をドラッグできる。画面見本では「条件の違う5枚を投入」で同じ動作を確認できる。複数画像もドロップ順に1枚ずつ読み取られる。
3. 1枚目の金額を `1001`、2枚目の日付を `2026-09-16` に変更する。
4. 読み取り失敗の3枚目へ、店名・金額・日付を手入力する。
5. 「5件・合計円を登録」を押す。
6. 上部の「履歴5件／取引10件」と「保存された履歴と取引を確認」を見る。3人参加で立替者を除く2人分の取引が各履歴に作られる。

### 結果不明と二重保存防止の手順

1. 「新しい架空例」を押し、保存応答を「保存後の応答断」にする。
2. 5枚投入後、1枚目以外を「登録から除外」にして1件を登録する。
3. 合計が1000円へ増え、カードが「保存を確認できません」になることを確認する。
4. 「保存結果を確認」を押す。保存済みへ変わっても合計1000円、履歴1件、取引2件のままであることを確認する。
5. ページを再読込して「イベントの支払いを追加」を押す。同じ保存済みカードが復元され、再登録されないことを確認する。

### 今回の代替と未確認

- OCR：SVGの架空レシートに対応する固定応答。実課金APIは未使用。
- 保存先：sessionStorage上の模擬Firestore。保存処理自体は実 `batchPaymentSave.js`。
- 通知：Callableのローカル応答まで。実FCM送信は未実施。
- スマホ実機：未確認。本人確認ではカメラ起動、アルバム5枚、HEIC、入力時キーボード、縦スクロール、通知受信を見る。

以下は作業途中の比較環境と設計経緯です。現在の統合確認は上記を優先します。

## 対象

実コンポーネント `AddPaymentModal.vue` / `ReceiptResultCard.vue` と、`evenShares.js` / `batchPaymentSave.js` / `batchStates.js`。

通常テストのコマンドは変更していません。専用テストを別途実行します。

```bash
node --test tests/receipt-flow/money.test.mjs tests/receipt-flow/modal.test.mjs
npm test
npm run build
```

- money: 独立した端数期待値、不正入力、IDの重複、日付、保存結果の4区分、付随処理の時間切れ。
- modal: 実コンポーネントのscriptをVueで読み込み、OCR応答・保存先・タブ内記憶だけを置換。3枚選択、編集、除外/復帰、保存、二重押し、閉じ直し、コンポーネント再作成、結果確認、同じIDでの手動再送を検証。
- 模擬保存のテストだけではFirestoreのルール/競合動作を保証しません。

## 画面確認

```bash
node tests/preview/receipt-flow/server.mjs
```

比較入口：`http://127.0.0.1:5187/`。同じ幅（320/390/600px）の変更前/変更後を並べます。

- 変更前：`?view=before&width=390`。作業開始コミット `9e8dc568074bf4a6791d35ffdd31cb30b6d2f060` の実AddPaymentModalをgitから仮想読込します。SHA256 `abe06c6495cf7858eb118213a20fb6d0e6504f31c3ae134246cd33510c79d84f` を起動時に照合します。開始時の未コミット差分はありませんでした。公開中の版との一致は未確認です。
- 変更後：`?view=after&width=390`。現在の未コミット実装を読み込みます。
- カード9状態：`?cards`。

両側とも同じ架空参加者と同じ3枚のOCR応答を使用します。1枚目1000円/2026-09-17、2枚目301円/日付不明、3枚目読取失敗。旧版に複数枚機能はないため、1枚ずつ選びます。旧版の画面を捏造せず、元のファイルを上書きしません。

**実部品と模擬部分：** フォームは実物。画像/OCRは架空。旧版のEventDetails側の保存処理は比較対象に含めず、フォームのsubmitを検証用の親が受け付けます。新版は実batchPaymentSaveと模擬データベースを組み合わせます。外部API/Firebase/本番データに接続しません。模擬データと実フォームの再開情報はこのタブのsessionStorageへ保存し、再読込→結果確認を試せます。実データの永続性を保証する検証ではありません。

### 触って確認する順番

1. 両側でフォームを開く。旧版は「選んだ1枚を投入」→比較条件を合わせるため「全員で均等」を選択。新版は「複数レシート」→「同じ3枚を投入」。旧版は1枚、新版は3枚の結果が表示されます。
2. 1枚目を1001円へ修正。新版2枚目の日付を2026-09-16へ入力。旧版の日付不明応答は既定日を残しますが、新版は補うまで要確認です。
3. 新版3枚目を除外→戻す→除外。読取失敗に戻り、最後に2件1302円が登録対象になります。
4. 新版を登録し「保存済み2件」と合計1302円を確認。旧版は1枚ずつ送信し、フォームを閉じます。両側の模擬合計を同じ1302円へできます。
5. 新版の「新しい架空例」→保存応答を「保存後の応答断」→同じ3枚を投入→日付等を補わず1件1000円を登録。結果不明のまま自動再送せず、「保存結果を確認」で保存済みへ変わり、合計1000円が増えないことを確認します。ページ再読込→フォームを開く→結果確認も可能です。

### 比較環境の確認記録（2026-09-17）

- 旧/新の実フォームを起動し、両側320/390pxをDOMで確認。同じ架空データで金額訂正と旧1枚ずつ/新2件まとめての1302円を確認。
- 新版の金額/日付訂正、除外→読取失敗へ復帰→再除外、保存中の編集ロック、結果不明→ページ再読込→再確認で合計1000円を維持することをブラウザ確認。
- 旧版は日付不明でも既定日が残ること、読取失敗のエラー表示を確認。旧親の保存処理は実行していません。
- 5187はこの作業専用。Viteキャッシュは `tests/preview/receipt-flow/.vite-cache` に隔離。共有サーバー/中央資料は変更していません。
- このURLはこのMac用。スマホ実機、課金OCR、イベント画面への統合、実際の保存先、本番配信の比較は未確認です。

## Firestore保存確認

`emulator.test.mjs` は `demo-receipt-flow`、`127.0.0.1:8087` 固定です。専用Firestoreエミュレータをこのプロジェクトの `firestore.rules` で起動してから実行します。共有ポートや本番設定を使わないでください。新しい架空イベントだけを作り、既存データを消しません。

```bash
RECEIPT_EMULATOR_TEST=1 node --test tests/receipt-flow/emulator.test.mjs
```

現行ルールで同一IDの同時送信、履歴/取引/合計の整合、終了済み・参加者変更・部外者の拒否、実保存後に応答だけ遮断した状態からの確認を検証します。課金OCRは実行しません。

## 接続契約と制限

親から `eventId: String`、`eventName: String`、`eventEnded: Boolean` を渡すと複数枚の入口が出ます。EventDetailsの接続差分は別担当です。支払いの保存は一括保存部品へ直接渡し、単枚の `submit` イベントを発火しません。

保存に入る前にIDと登録内容を固定し、イベント/ログイン利用者別のsessionStorageへ保持します。同じタブでの閉じ直し・ページ再読込で再開可能です。タブを閉じる、ブラウザのサイトデータを消す、別端末へ移る操作までの再開は保証しません。結果不明は自動再送せず、確認操作だけを提示します。

再開データの破損/容量不足は送信を止めます。保存失敗のカードも元のID/内容を保持し、勝手に編集可能な新規カードへ戻しません。参加者変更などで再送が継続できない場合の破棄/再計画は未実装です。

支払いが保存済みでもチャットの反映確認が失敗することがあります。現行ルールでは存在しないチャットの読取が拒否されるため、応答断後のチャット補完ができない場合があります。保存済み金額を再登録せず警告を残します。ルール/チャット処理の変更は別担当との検討事項です。

実スマホのファイル選択/キーボード、課金OCR、統合後のイベント画面・実データ・本番反映は別途確認が必要です。

## 2026-09-17 追加要求：撮影と各写真の情報保持

本人の依頼に合わせ、「1枚撮って追加」とアルバムで最大3枚選択する入口を用意。現時点の仮定は写真1枚につきレシート1枚です。複数レシートを1枚の写真から分離する機能は含みません。

- 読取は直列。順番待ち/現在の枚数を表示し、サーバーの120秒制限に合わせてクライアントは130秒まで待ちます。画像を結合・縮小・再圧縮しません。4MBを超える画像は現行上限で拒否し、手入力を案内します。
- 店舗・日付・時刻・通貨・合計・ポイント・税区分・登録番号・明細（行合計/数量/税率）をカードの`receipt`に保持し、読み取り詳細と元画像を表示します。日付/時刻不明を現在の値で埋めません。
- 保存する`receipt`は読み取り時点の控えです。金額/日付を修正した場合の登録値とは区別します。従来の`items`へも金額が判明した行を渡し、数量を二重に掛けません。金額不明の行は0円へ変換せず`receipt`に残します。精算額は修正後の合計を全員均等で配分します。
- 写真は画面確認中のメモリにだけ保持し、Firestore・再開用sessionStorageには保存しません。読み取り項目と固定保存計画は再開できます。既存の他画面で`receipt`の全項目を表示・編集する接続は範囲外です。
- 専用テスト19件、既存46件、build成功。追加テストで3枚の直列処理、元画像のままの引渡し、異なる時刻/日付、値引明細、数量、税抜、読取不明、修正値との区別、保存/再開を確認。320pxの実部品で詳細の横はみ出しなし、架空2件1302円の保存をブラウザ確認。
- 専用Firestoreエミュレータの4シナリオ（親を含む5件）も再実行成功。現在のrulesで登録番号・空の時刻・不明値を含む`receipt`が保存されることを追加確認しました。既存のチャット反映の制限は残ります。

スマホの機種・ブラウザは本人への確認待ち。このMacのブラウザ幅検証はスマホ実機確認ではありません。カメラ起動、3枚選択、HEIC等の実画像形式、4MB超の写真、キーボード、実際のOCR精度は未確認です。現行5187はloopback限定でスマホからアクセスできません。実機確認の接続方法と、実画像を送信する場合の対象環境を決める必要があります。

## 2026-09-17 再設計案：単枚との統一と他ページへの個別反映

本人から単枚の撮影UIの流用、3枚制限の理由の再検討、1枚ずつ撮影する説明、履歴/精算/チャット/お知らせをレシートごとに扱う案を受領。以下は設計案で、既存の3枚制限等を実装上変更したとは扱わない。

### 確認した事実

- 3枚は旧設計図§2-3の展示向け最小案（待ち時間・費用・縦に並ぶカード数）に由来。現行バックログ§4も本人確定仕様ではないと明記。技術上の3枚固定上限という根拠はない。旧実測の待ち時間を現在の保証値として使わない。
- 作業版の`batchPaymentSave`は1枚ずつ独立した履歴ID・取引を保存し、その履歴IDに対応した支払いチャットを作る。相手の負担がない支払いにはチャットを作らない。
- 作業版の単枚`EventDetails.addHistory`は新規登録だけで通知を作らず、編集時に`payment_edited`を作る。複数保存の付随処理も現在はチャットだけ。チャット作成と未読増加・お知らせ作成は同一ではない。
- `functions/index.js`の`pushOnNotification`はお知らせ文書1件の作成に対してプッシュ送信を呼ぶ。受信側の通知許可等によって実受信は異なるが、新規お知らせ4件を作る変更はプッシュ4回の送信経路にもなる。

### 画面案と変更の単位

操作見本は `http://127.0.0.1:5187/?proposal`、ファイルは `tests/preview/receipt-flow/FlowProposal.vue`。既存の単枚撮影アイコンと既存の色/角丸を流用。これは架空データの画面案であり、実カメラ・OCR・保存・通知・割り勘計算は動かさない。実コンポーネントの比較ページは残す。

1. 入口を「レシートで支払いを追加」にそろえる。カメラは「1枚ずつ撮影」、アルバムは複数選択可能で読み取りは順次、という説明を残す。
2. 1枚の写真と単枚と共通の入力画面で、店名・金額・日付・時刻・立替者・割り勘・明細等を確認する。単枚フォームを全部コピーして別管理するのではなく、撮影/プレビュー/入力欄を共通化する実装方針。登録待ちの新規下書きに既存支払いの`editData`を流用しない。
3. 「確認して一覧に追加」→次を撮影。確認済みは店名・日付・金額・立替者・方式を短い行で表示し、選んだ1枚だけ詳細へ戻れる。4枚目で止めない。アルバムの大量選択・画像メモリ・回数制限は個別に計測し、必要な制約が判明した場合は理由付きで扱う。「無制限に安全」とは約束しない。
4. 最後に「N件を個別に登録」。押す操作は一度でも、保存と状態は1枚ずつ。1件の保存不明で全件を送り直さない。

### 他ページとの接続案

| 反映先 | 単位・期待動作 | 必要な確認 |
|---|---|---|
| イベント履歴・履歴詳細 | 4枚なら独立した4件。店名/日時/明細/設定をそれぞれ保持 | 1件の編集・削除等で他3件を変えない。再編集で元の読取控え`receipt`を落とさない（既存単枚の丸ごと上書きへの接続が必要） |
| 精算の内訳・申請/承認 | 各履歴に属する人別の取引が独立。既存の明示的なまとめ精算は別機能 | 1枚分だけ精算して他3枚の未精算を保つ。人別の合計表示から各明細へ戻れる |
| 支払いチャット | 原則1レシート1件。支払う相手のいないレシートは作らない | 件名/参加者/金額/状態/詳細の行先が対象の履歴と一致。保存の再確認で会話・未読を初期化しない |
| アプリ内お知らせ | お知らせが必要な操作をレシートごとに追える。新規登録時の追加は本人へ確認中 | 本人を除く関係者、historyId/対象取引/遷移先、既読を独立させる。4枚に関係する人なら4枠。無関係な人へ送らない |
| スマホのプッシュ | アプリ内お知らせとは分けて検討 | 新規お知らせを追加する場合、現行の連動で最大4回送信される点を確認する。集約するなら別の送信設計が必要 |
| ホーム等の集計 | 人別・イベント別の合計表示は維持し、元明細4件を保持 | 複数取込だけで別の集計式にしない。全画面に機械的に4枠を足す変更はしない |
| 登録直後の結果表示 | 「4件保存しました」等の結果は1回表示してよい | これは相手への通知の集約でも、4件の精算の合体でもない。失敗/不明を個別に残す |

新規登録時にもお知らせを追加するかを本人へ質問済み。追加すると相手へ届く動作が増えるため、回答前に通知生成や送信経路を変更しない。共有の履歴/精算/通知/チャット画面は他担当の所有であり、本タスクでは編集していない。

今回の画面見本では、320pxで横はみ出しなし、1枚の確認→一覧、4件中1件の金額変更（2380→2381）が他3件へ波及しないこと、4件の反映例をブラウザで確認。実アプリの計算・保存・通知や実機の合格とは扱わない。今回の改善点は、旧最小案の3枚・均等固定を本人確定仕様のように持ち込まないこと、入力の枚数と他ページのレコード単位/通知回数を先に分けて設計すること。

### 本人の再補足と推奨案（同日・上記の未確定事項を更新）

本人は単枚入力と複数レシートの入口/操作を別として扱うこと、部品の流用は可、実用上の枚数上限を検討すること、UIの判断を任せることを明示。追加時のお知らせは存在する前提だったが、ないなら追加を検討してほしい、ただし通知のうるささも懸念、という依頼。前の「入口をそろえる」は一機能への統合ではなく、別入口のまま撮影/写真プレビュー/入力部品を共通化する方針へ訂正。

- **推奨UI：** 単枚は1件入力→登録、複数は撮影/アルバム→1枚ずつ確認→登録待ち一覧→個別保存。撮影/入力部品を共通化しても下書き・保存・復旧状態は混ぜない。
- **上限の初期案：1回5枚。** 4枚以上の用途にも応え、待ち時間・画像メモリ・誤操作の範囲を区切るための設計判断。実測で最適と判定した数字ではない。5枚を登録した後に次を追加できる。1回の枚数、1枚の容量、利用全体の回数制限を分ける。実機計測前に3枚→5枚を本体へ反映したとは扱わない。画像の圧縮や永続保存は追加しない。
- **推奨通知：アプリ内は1レシート1件、端末のプッシュは1回の登録操作・受信者につき要約1件。** 4件すべてに関係する相手なら、アプリ内4件、プッシュ1件。自分や無関係な人には送らない。チャットと支払い自体は独立した4件を維持する。
- 単枚追加も追加のお知らせを作る案。追加のプッシュだけオフにでき、アプリ内記録は残る設定が望ましい。承認依頼/拒否等の対応が必要な通知は追加通知の集約へ巻き込まない。これらは提案で、通知送信の実行許可・実装済みとは扱わない。
- 現コードは`notifications`の各作成でプッシュを呼ぶため、**表示上4件をまとめるだけでは通知連発を防げない**。アプリ内用の個別通知とプッシュ用の送信経路をサーバーで分離する変更が必要。ネイティブOSの自動グループ化やFCMの未配信メッセージ置換に、1回の送信保証を依存させない。
- 保存をサーバーで確認できた件だけ通知対象にする。保存不明は成功件数へ含めない。受信者・履歴IDで個別通知、受信者・登録操作IDで送信管理を識別し、再確認/再試行で追加通知を増やさない。途中でアプリが閉じても動くサーバー側の確定・再開処理が必要で、フロントの最後に1回呼ぶだけでは不十分。外部push配信の厳密なexactly-onceは保証しない。要約通知の遷移先で各レシートを確認できることも完成条件にする。
- 本体/Functions/通知表示・設定は今回未編集。専用の画面案のみ、別入口の案、5枚の仮上限、個別お知らせ＋プッシュ要約1件の見本へ更新。

参照した公式指針： [Apple Notifications](https://developer.apple.com/design/human-interface-guidelines/notifications/)（同じ事柄の通知連発を避ける）、[Androidの通知グループ](https://developer.android.com/develop/ui/views/notifications/group)（個別内容を保持しつつ要約だけで知らせる設計）、[FCMの置換可能メッセージ](https://firebase.google.com/docs/cloud-messaging/customize-messages/collapsible-message-types)。OS固有APIをSettloのWebアプリへそのまま使えるという意味ではなく、通知の情報量と割り込み回数を分ける設計の参考。5枚という数値はこれらの公式指針から導いたものではない。
