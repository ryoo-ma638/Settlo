# Settlo 開発メモ（チーム共有用）

このドキュメントは、大崎が行った開発作業を、**他のメンバーが把握・引き継げるように**まとめたものです。
最終更新: **2026-07-01**（実機テストで判明した課題を反映）

---

## 0. いまの状態（まずここを読む）

- **本番公開URL： https://settlo-app.web.app** （Firebase Hosting・サイト名 `settlo-app`／プロジェクトID `pairpay-4c17a`）
- **GitHub： `ryoo-ma638/Settlo`（公開リポ）。`main` ＝動く状態**。`main` は**PR必須の保護**あり（直push不可）。
- UIリデザイン・実データ連携は**すでに main にマージ済み＆本番反映済み**（※以前の「まだpushしていない」は解消済み）。
- 漏えいした鍵は**全て無効化済み**で実害なし。履歴書き換え（force-push）も対応済み。

### ローカル開発の始め方
1. **最新リモートから clone**（古いローカルで `git pull` は厳禁・履歴書き換え済みのため事故ります）。
2. ルート直下に **`.env.local`** を置き、`VITE_FIREBASE_*` を設定（`.env.dev` という名前だとViteが読まず `auth/invalid-api-key` で起動失敗）。
3. `npm install` → `npm run dev`（http://localhost:5173）。

### デプロイ（CI未設定・手動）
```bash
npm run build
firebase deploy --only hosting --project pairpay-4c17a   # → settlo-app.web.app
firebase deploy --only firestore:rules --project pairpay-4c17a  # ルール変更時
```
- ⚠️ `.env.local` / `service-account.json` は**絶対コミットしない**（`.gitignore` 済み）。
- Gemini APIキーは**Secret Manager**管理（`firebase functions:secrets:set GEMINI_API_KEY`）。コードに直書きしない。

---

## 1. これまでに完了した対応

### 1-A. 情報漏えい対応（セキュリティ）
- 公開リポのGit履歴に `service-account.json`（Admin秘密鍵×3）と Gemini APIキー×7 が混入していた。
- 鍵は**全て無効化/失効済み（実害なし）**。`git filter-repo`＋force-pushで全履歴から除去 → **全員 re-clone 必須**。
- 公開リポに **Secret Scanning＋Push Protection** を有効化。残る公開キー（Firebase Webキー・VAPID公開鍵）は公開前提で安全。

### 1-B. Firestoreルールの厳格化
- テストモード初期ルールが2026-04-24で期限切れ→全書き込み拒否だった問題を解消。
- `firestore.rules`（**コレクション単位の厳格版**：当事者/イベント参加者のみ読み書き）を作成し**デプロイ済み**。`firebase.json` 管理下。

### 1-C. UI全面リデザイン（PayPay風・モバイルファースト）
- ブランド緑統一／絵文字全廃（SVG化）／和欧フォントNoto Sans JP統一／金額 `¥1,234`（空白なし・等幅）。
- 設計基盤＝`src/assets/base.css`（トークン）＋`main.css`（再利用クラス）。共通部品 `PageHeader.vue` / `GenreIcon.vue`。
- 骨格＝常時モバイルシェル（`App.vue`＋`AppHeader`＋`AppFooter`＋中央「＋」FAB）。3カラム/サイドバーは廃止。
- 詳細は本書「4. 設計リファレンス」。

### 1-D. 実データ連携（2026-07-01）
固定の偽データ（小野木/天野＝元メンバー）を実データに置き換え：
- **`InviteModal.vue`**：本物のフレンド一覧（`users/{uid}/friends`）を購読＋名前/UIDで全ユーザー検索。招待で相手UIDを**イベントの `participants` に実追加**（`arrayUnion`）。既参加者は「参加済み」。
- **`AddPaymentModal.vue`**：「立替えた人」を**イベントの実参加者から選択**（初期値＝自分）。`payerUid` も親へ渡す。
- **`EventDetails.vue`**：参加者名を**実データ表示**（"メンバー"固定を解消）。立替者UIDで債権を記録。自分の表示名も実データ。
- **`MakeEventView.vue`**：上部に「**作る／参加する**」タブを追加（参加導線を可視化）。`joinEvent`（招待コード参加）は実装済み。
- **招待コードの常時表示＋コピー**：イベント詳細にバーを設置（作成後でもコピー可）。
- **アバター歪み修正**：丸枠＋`overflow:hidden`＋`object-fit:cover`方式に変更（縦長楕円を根治）。48pxに拡大・重なり表示。
- **`showAlert` 修正**：`alertState` がどのモーダルにも繋がっておらず無反応だった潜在バグを修正。
- `firebase.json` に `"site": "settlo-app"` を追加（PR #107）。

### 1-E. 支払い機能の拡充・通知（2026-07-01 追補）
- **割り勘ロジック修正**：`custom`（金額指定）/`item`（商品ごと）の負担額を、モーダルが算出する `shares`（uid別）どおりに transaction 化。精算サマリー（`useSettlement`）も `shares` を正データに統一。
- **支払いごとのジャンル**：食事/カフェ/コンビニ/交通など10種を選択（`GenreIcon` にアイコン追加）。立替履歴・決済画面のアイコンに反映。
- **支払いの編集・削除**：決済詳細（`ReceiptPaymentModal`）に「編集／削除」。編集は内容プリフィル→保存で作り直し（`addHistory` の `editId`）、削除は確認つきで transactions/履歴/合計を消す。
- **変更通知（差分つき）**：支払いの編集・削除、イベント編集（名前/ジャンル）時に参加者（自分以外）へ通知。`notifications` の `payment_edited`/`payment_deleted`/`event_edited`。通知に「金額 ¥100→¥200」等の before→after を表示。
- **精算サマリー/決済画面**：矢印の両側を実人物アイコンに。イベント上部を「未精算の残り」＋進捗バー（X/Y件・%）に。決済画面は閲覧者目線の金額＋「誰がいくら」内訳。
- **Cloud Functions CORS修正**：`analyzeReceipt`/`calculateSettlement` の CORS に `settlo-app.web.app`（と `.firebaseapp.com`）を追加。公開URL変更で関数呼び出しがブロックされていたのを復旧。要 `firebase deploy --only functions`。

### 1-F. イベント招待の承認制化（2026-07-01 追補）
無断で参加者に追加される仕組みをやめ、相手の同意を挟むフローに変更：
- **招待は通知を送るだけ**：`InviteModal` は `participants` への即 `arrayUnion` をやめ、相手に `event_invite` 通知（イベント名・招待者名つき）を送る。
- **受け取り側で選択**：通知に「参加する／心当たりがない」を表示（`NotificationIcon`）。「参加する」で初めて自分を `participants` に追加し、イベントへ遷移。
- **拒否は二段階確認**：「心当たりがない」→「本当に心当たりがないですか？」と再確認 → 拒否で招待した人へ `invite_rejected` 通知（「内容が正しいか再確認を」）。
- ルール変更は不要（`events` の update は「自分を参加者に追加する更新（join）」を既に許可済み）。

### 1-G. 参加通知・ゴミ箱（復元）機能（2026-07-02 追補）
- **参加通知**：誰かがイベントに参加（招待承認＝`NotificationIcon.acceptInvite` / 招待コード＝`MakeEventView.joinEvent`）すると、既存メンバー全員へ `event_joined`（「Xさんが『Y』に参加しました」）を送る。
- **ゴミ箱（`/trash`・`TrashView.vue`・MyPageから）**：削除や完了の取り消しを7日間可能に。
  - **イベント削除＝ソフト削除**：`deleteEventCompletely` は完全削除をやめ、`events.hiddenBy` に自分を追加＋`users/{uid}/trash` に記録（**自分の画面からだけ非表示**・相手や貸し借りは残る）。イベント一覧（`HomeView`/`EventViews`）は `hiddenBy` に自分がいる物を除外。
  - **イベント復元**：`hiddenBy` から自分を外す＋他メンバーへ `event_restored` お知らせ（承認は不要）。
  - **決済完了＝ゴミ箱に記録**：`markAsCompleted`（イベント）と `CombinedActionView`（フレンド精算）で完了時に `type:'settlement'` の trash を作成。
  - **決済の未精算戻し＝相手の承認制**：ゴミ箱で「未精算に戻す」→ 相手（counterparties）へ `settlement_restore_request`、trash は `status:'pending'`＝**「保留」タブ**へ。相手が承認で対象 transaction を `unpaid` に戻す（`settlement_restore_approved`）／拒否で完了のまま（`settlement_restore_rejected`）。
  - **自動削除**：`functions/index.js` の **スケジュール関数 `purgeTrash`**（毎日 04:00 JST・`onSchedule`）が `collectionGroup('trash')` の `trashedAt` 7日超を本削除。要 `firebase deploy --only functions`（初回は Cloud Scheduler の有効化確認が出る）。
  - ルール追加：`users/{uid}/trash` は本人のみ read/write。

### 1-H. 通知・ゴミ箱・入力の改善（2026-07-02 追補2）
- **友達申請に「拒否」ボタン**（`NotificationIcon`）。承認/拒否の要求系通知は、どちらか選ぶまでお知らせに残る形に統一（招待・決済戻し・友達申請）。
- **削除した支払いもゴミ箱へ**：`deletePayment` は取引・履歴の控え（`historySnapshot`/`transactionSnapshots`）を `type:'payment'` で trash に保存してから削除。ゴミ箱の「元に戻す」で取引・履歴を作り直す（`TrashView.restorePayment`）。
- **イベント終了の確認を1つに**：未精算ありの終了は確認モーダルを二重に出していたのを1つに統合。
- **金額不一致は保存させない**：`AddPaymentModal` で内訳/指定の合計が全体と合わない時、差額を示して「直してから保存」を促す（登録・編集の両方・以前の「このまま追加」は廃止）。
- **事業者登録番号（インボイス）**：レシートOCR（`analyzeReceipt` プロンプト）で `registrationNumber`（T+13桁）を抽出→`AddPaymentModal` に表示・手入力も可→履歴に保存。
- `.gitignore` を強化（`.env` / `.env.*` を明示）。※環境変数の直書きは無し・`firebase.js` は `import.meta.env` から読む。

### 1-I. 税計算・残金・PayPay・イベント終了/削除（2026-07-03 追補3）
- **支払い追加の無反応バグ修正**：`AddPaymentModal` が `BaseModal` を描画しておらず警告が出ないまま処理停止していた（PR#117）。
- **税の計算方法（3方式・自動判定）**：①価格は税込 ②税抜→合計に課税（税率ごとにまとめて1回だけ課税＝1円ずれ防止）③税抜→商品ごと課税。レシートOCRが `taxIncluded` を判定して自動選択・商品ごとの税率（8/10）ボタンもプリセット（PR#121）。OCR結果はクランプ検証（金額/個数/文字列・登録番号はT+13桁のみ）。
- **不明な残金パネル**：内訳合計と会計総額を画面下に常時表示。差額があれば「¥X 足りません。どうしますか？」→**負担する人**（既定＝立替者）と**理由**（任意）を選んで保存（PR#122）。割り勘合計は必ず総額に一致。
- **支払い完了通知**：受け取り側の承認・現金受け取り・まとめ精算の完了時に、相手へ `payment_completed`「支払いが完了しました」を送信（PR#123）。PayPayは `PayPayAction`（リンク登録→相手がリンクから支払い→承認）で完結。
- **イベント「終了」と「削除」を分離**（PR#124）：
  - **終了**＝精算をすべて済ませて締める（`ended: true`・記録として残る・全員に「終了済み」表示・新規支払い追加は不可）。未精算が残っていれば精算サマリーへ誘導。
  - **削除**＝自分の画面から非表示（`hiddenBy`＋ゴミ箱・7日以内復元可）。
- **ゴミ箱UI刷新**：タブのスタイル欠落を修正しアイコン+件数バッジの大きめタブに（PR#120）。

### 1-J. 承認システムの拡張・共有ゴミ箱・脆弱性修正（2026-07-03 追補4）
- **取引申請のインライン承認/拒否**（PR#126）：`approval_request` 通知に「承認する／拒否する／詳細」。選ぶまでお知らせに残る。承認＝完了+`payment_completed`、拒否＝未払いに戻す+`approval_rejected`。
- **共有ゴミ箱**（PR#127）：取引（支払い削除・決済完了）の控えを `trash` コレクション（`participants`＝当事者・ルールで当事者のみ）に保存し**両当事者が見られる**。復元すると相手へ `restore_check`「元に戻されましたが正しいですか？」→「正しい」＝確定／「正しくない」＝復元を取り消して**ゴミ箱へ差し戻し**+`restore_reverted` 通知（双方のデータが必ず一致）。復元直後は保留タブ「復元の確認待ち」。イベントの削除は従来どおり自分専用ゴミ箱。
- **一方的な削除・復帰の確認**（PR#128）：イベント削除時に他参加者へ `event_left_check`「〇〇さんが抜けました。正しいですか？」（正しくない→削除者へ `event_left_rejected`）。イベント復元通知（`event_restored`）にも承認/拒否（拒否→復元者へ `event_restore_rejected`）。
- **支払い完了通知**：承認・現金受領・まとめ精算time完了時に相手へ `payment_completed`（PR#123）。
- **脆弱性修正**（PR#125）：フロント20件→0件（firebase v10→v12）・functions 23件→8件（すべてGoogle SDK内部・上流待ち）。

---

## 2. やること（タスク）

課題・TODO は **[TASKS.md](./TASKS.md)** に優先度つきでまとめています
（招待の承認制化・支払いの指摘/異議機能・フレンド導線の改善・seed残骸の掃除 など）。

---

## 3. データモデル（要点）

- `users/{uid}`：`uid` / `name`(displayName) / `email` / `photoURL` / `lastLogin`。**検索用の独自IDは無く、ID検索はFirebase UIDそのもの**。
- `users/{uid}/friends/{friendUid}`：`uid` / `name` / `photo` / `isFriend` / `isTrading` / `tradeCount` / `addedAt`。
- `friendRequests`：`toId` / `formId`(=fromId) / `formName` / `formPhoto` / `status`(pending/accepted)。
- `events/{eventId}`：`name` / `participants`(UID配列) / `invitationCode` / `totalAmount` / `tag` / `memo`。
  - サブコレクション `history`：立替履歴（`payer`名 / `amount` / `splitType` / `items` / `transactionIds` …）。
- `transactions`：`paidById`(債務者) / `paidToId`(債権者) / `amount` / `status`(unpaid→awaiting_approval→completed) / `eventId` / `itemName` / `createdAt`。**MoneyPage等が読む正データ**。
- `notifications`：`toUserId` / `type`(approval_request/approval_rejected/payment_reminder…) ほか。
- Cloud Functions（asia-northeast1）：`analyzeReceipt`（Gemini OCR）/ `calculateSettlement`（最小送金）。

---

## 4. 設計リファレンス（UI）

- **トークン**：`src/assets/base.css`（色 `--c-brand`=緑 / `--c-pay`=アンバー(支払) / `--c-receive`=緑(受取)、タイポ、余白、角丸、影）。フォントはNoto Sans JP統一。
- **再利用クラス**：`src/assets/main.css`（`.card` `.btn-brand` `.btn-outline` `.btn-trash` `.seg` `.screen-head` `.row-card` `.tag` `.empty-box` など）。**新規UIはこれらを使い回す**。
  - ※ `main.js` で `import './assets/main.css'` 済み（以前は未importでCSSが死んでいた）。
- **共通部品**：`PageHeader.vue`（戻る＋タイトル, sticky top:0）/ `GenreIcon.vue`（ジャンルSVG）。
- **アバターは「丸枠div＋overflow:hidden＋中の img を object-fit:cover」**で描く（imgを直接flex子要素にすると縦長に歪むため）。`FriendView` / `EventDetails` がこの方式。
- 注意：`base.css` に **グローバル `img { display:block; max-width:100% }`** があるので、画像は必ず丸枠の中に入れる。

---

## 5. 触ると危ないポイント

- **古いローカルから push しない**（履歴書き換え後のリモートとズレている）。mainへの反映は**きれいな再clone → 新ブランチ → PR**で。
- ルール変更は本番Firestoreを上書きするので、デプロイ前に現行ルールを確認。
- `service-account.json` を使った管理操作（seed/admin）は本人が手元で実行（鍵は厳重管理）。
