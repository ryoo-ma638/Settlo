# Settlo タスク一覧（やることリスト）

実装の優先順位つき。最終更新: **2026-07-01**。
※ 設計・セットアップ・データモデルは [開発メモ.md](./開発メモ.md) を参照。

---

## 🔴 高（バグ・コア機能・実利用で破綻する）

- [ ] **割り勘ロジックの修正** ⭐最優先
  - 症状: 100円を A=70 / B=20 / C=10 と指定しても、C に「100円決済して」と出る（指定額が無視される）。
  - 原因: `EventDetails.addHistory` が `splitType==='all'` 前提で均等割りの transaction しか作らず、`custom`（金額指定）/`item`（商品ごと）を `isProvisional` でごまかしている。
  - 対応: custom/item の負担額を debtor ごとに正しく transaction 化する。
  - 関連: `src/views/EventDetails.vue` / `src/components/AddPaymentModal.vue` / `src/composables/useSettlement.js`

- [ ] **支払い追加モーダルのモバイルUI崩れ**
  - 日付と時間のパネルがくっつく・ヘッダーが狭い。スマホ幅でレイアウト調整。
  - 関連: `src/components/AddPaymentModal.vue`

- [ ] **身に覚えのない過去取引が表示される（要調査・掃除）**
  - フレンド承認時などに、取引していないのに過去取引が2件ほど出る。テストデータ(seed)残骸の可能性が高い。
  - 対応: 本番 Firestore の `transactions` を確認し、不要データを除去（※本番データ操作は大崎が実行）。

---

## 🟡 中（UX・機能追加）

- [x] **イベント招待を「承認制」にする（無断追加の防止）**（2026-07-01 実装）
  - 済：招待は即追加をやめ、相手に `event_invite` 通知を送るだけに（`InviteModal.vue`）。
  - 済：受け取った人は通知の「参加する／心当たりがない」で選択（`NotificationIcon.vue`）。「参加する」で初めて自分を `participants` に追加。
  - 済：「心当たりがない」→「本当に心当たりがないですか？」と再確認 → 拒否で招待した人へ `invite_rejected` 通知（正しいか再確認を促す）。
  - 関連: `notifications` / `src/components/InviteModal.vue` / `src/components/NotificationIcon.vue` / `src/views/EventDetails.vue`

- [x] **参加者一覧から各参加者をタップ → フレンド申請へ**（2026-07-05 実装）
  - 済：参加者一覧の各行に「フレンド申請」ボタン（フレンド済み／申請済みは状態タグ表示）。
  - 関連: `src/views/EventDetails.vue`（参加者一覧モーダル）

- [x] **フレンド検索の改善（部分一致・候補表示）**（2026-07-05 実装）
  - 済：名前検索を**先頭一致**の候補表示に。**同じイベントのメンバー**は部分一致でも候補に出し、検索欄が空のときは候補として一覧表示（フレンド済みは除外・バッジ表示）。
  - 関連: `src/components/FriendAddModal.vue`

- [ ] **CombinedSettlementView の集計を精査**
  - 実データ集計になっているか確認（ダミー名のハードコードは見当たらないが要確認）。
  - 関連: `src/views/CombinedSettlementView.vue`

- [x] **イベント・参加者・支払いの編集機能**（2026-07-01 要望）
  - 済：イベント名・ジャンルの編集、参加者を外す（確認つき）。
  - 済：**支払いの編集・削除**（決済詳細モーダルの「編集／削除」。編集は内容プリフィル→保存で作り直し、削除は確認つきでtransactions/履歴/合計を消す）。
  - 済：**編集・削除時に関係者へ通知**（`payment_edited`/`payment_deleted`）。
  - 済：**変更差分の可視化**（通知に「金額 ¥100→¥200 / ジャンル 食事→カフェ」等の before→after を表示）。
  - 済：**イベント自体の編集（名前/ジャンル）も参加者へ通知**（`event_edited`・差分つき）。

- [ ] **支払い内容への「指摘（異議）」機能**（2026-07-01 要望）
  - 編集/削除や金額に納得いかない時、関係者が「内容が違う」と指摘（異議申し立て）でき、相手に通知が届く仕組み。承認/催促と同様の双方向フロー。

- [ ] **誤操作を防ぐ確認ダイアログの強化**（2026-07-01 要望）
  - イベント追加・参加者追加・支払い追加・決済などの前に「この内容でよいですか？」の確認をしっかり入れる。
  - 済：フレンド承認キャンセル時に「心当たりのない人ですか？」確認→拒否（削除）。

---

## 🟢 低（未実装機能・整理）

- [ ] プロフィール画像アップロード（Firebase Storage 連携）。`src/views/EditProfileView.vue` 96行が `// TODO` のまま。
- [ ] FCM 実プッシュ通知（今はトークン取得が `console.log` のみ・保存も送信もしていない）。
- [ ] `src/components/AppSidebar.vue` 削除（デッドコード・どこからも未参照を確認）。
- [ ] 旧バックエンド残骸削除（`src/index.ts` `src/routes/` `src/services/` `src/middleware/`）。直Firestore移行で不要。※削除は要合意。
- [ ] バンドル分割（本番JSが単一チャンク ~600KB・`manualChunks`）。
- [ ] **READMEの仕上げ**（バッジ・スクリーンショット・ソーシャルプレビュー画像・LICENSE）＝**アプリ完成後**に。※GitHubのAbout（説明・Website・Topics）は設定済み。

---

## ✅ 完了済み（参考・2026-07-02）

- **参加通知**（誰かがイベントに参加すると既存メンバーへ「Xさんが参加しました」＝`event_joined`）
- **ゴミ箱／復元機能**（`/trash`）：イベント削除＝自分だけソフト削除→7日以内復元・お知らせ／決済完了＝7日以内に「未精算に戻す」（相手の承認制・保留タブ）／毎日サーバーで7日超を自動削除（`purgeTrash`）
- イベント終了の確認強化・招待確認のz-index修正・通知リスナーのpermission-denied修正
- イベント招待の承認制化（`event_invite`／`invite_rejected`・二段階確認）

## ✅ 完了済み（参考・2026-07-01）

- 割り勘ロジック修正（custom/item の指定額どおりに精算）
- 支払いごとの**ジャンル選択**＋ジャンルアイコン表示
- 立替履歴の役割表示（受取待ち¥◯◯／自分の負担額）・UID判定
- 決済画面を自分目線の金額＋「誰がいくら」内訳に
- 精算サマリーに実人物アイコン（写真）
- イベントのジャンルアイコン表示
- イベント上部を「未精算の残り」＋**進捗バー（X/Y件・%）**に
- フレンド申請/承認の両画面に**本物の取引履歴**を表示
- フレンド承認キャンセル時の確認→拒否（削除）
- 偽の取引履歴（直書き）を削除
- AddPaymentModal 実データ化（立替者を実参加者から選択）
- InviteModal 実データ化（実フレンド＋ユーザー検索＋イベントへ実追加）
- 参加者名を実データ表示（"メンバー"固定を解消）
- アバター歪み修正（丸枠＋`overflow:hidden`・拡大・重なり）
- イベント作成画面に「作る／参加する」タブ
- 招待コードをイベント詳細に常時表示＋コピー
- `showAlert`（通知）が無反応だった潜在バグ修正
- Firestoreルール厳格化・デプロイ
- 情報漏えい対応（鍵無効化・履歴除去）
- 開発メモ.md 全面刷新／TASKS.md 新設
