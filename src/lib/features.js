/**
 * 機能の出し分け（フラグ）。
 *
 * ここは「出すか、隠すか」を決める1箇所です。環境変数ではなく定数の直書きにしています。
 * `.env.local` は git 管理外で、どちらの状態でビルドしたのかを後から追えないためです。
 * 定数なら git の差分に残り、PRの1行で状態が確定します。
 */

/**
 * 複数レシート一括読み込みの入口を出すかどうか。
 *
 * **この1行を `true` にすると一括機能の入口が出ます。`false` なら完全に隠れます。**
 * 撤退するときはここを `false` に戻して再ビルドするだけです
 * （`npm run build` → `firebase deploy --only hosting --project pairpay-4c17a`）。
 *
 * 既定は `false`（オフ）です。まだ中身ができていないので、出す準備が整うまで隠します。
 * 判定に使う場所は2箇所だけに限ります。支払い追加モーダルの入口ボタンの `v-if` と、
 * イベント詳細に置くシートの `v-if` です。ボタンが消えればシートは開かれません。
 *
 * まだどこからも参照していません（このファイルは土台として先に置いてあります）。
 *
 * @type {boolean}
 */
export const BATCH_RECEIPT_ENABLED = false
